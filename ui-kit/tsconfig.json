{
    "extends": "../tsconfig.json",
    "compilerOptions": {
        /* 👇 override the global “types” entry so the build ignores jest-dom */
        "types": []
    },
    "include": [
        "src/**/*"
    ]
}